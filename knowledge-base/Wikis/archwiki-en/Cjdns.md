# Cjdns

Cjdns implements an encrypted IPv6 network using public-key cryptography for address allocation and a distributed hash table for routing. This provides near-zero-configuration networking, and prevents many of the security and scalability issues that plague existing networks.

## Installation
Install the  package.

## Configuration
On installation, a  is autogenerated.

If you need to re-generate it, execute:

 # cjdroute --genconf > /etc/cjdroute.conf

Edit  as root to add at least one peer to the  section. Peers can be anyone else running cjdns who gives you credentials to connect to them with. They can be found in Hyperboria peers, #cjdns and #projectmeshnet on EFnet among many other places.

## Starting
Start/enable the .

If cjdns did not start, you should check the unit status or use journalctl and edit  if needed.
