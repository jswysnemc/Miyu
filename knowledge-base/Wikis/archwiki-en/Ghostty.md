# Ghostty

Project's Website:
Ghostty is a fast, feature-rich, and cross-platform terminal emulator that uses platform-native UI and GPU acceleration.

## Installation
Install the  package.

## Configuration
The configuration file, , is loaded from these locations in the following order. Configuration is optional and if no configuration file is found, Ghostty will use its defaults.

#
#  if XDG_CONFIG_HOME is not defined

The full default configuration, . Best viewed with the  command.

 $ ghostty +show-config --default --docs | less

## Basic syntax
## Font
